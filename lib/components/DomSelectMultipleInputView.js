import React from 'react';

import DomSelectInputView from './DomSelectInputView';

const DomSelectMultipleInputView = props => (
  <DomSelectInputView {...props} multiple/>
);

export default DomSelectMultipleInputView;
