import wrapInput from '../hoc/wrapInput';
import createViewContainer from '../hoc/createViewContainer';

const TextareaInput = createViewContainer('TextareaInput');

export default wrapInput(TextareaInput);
